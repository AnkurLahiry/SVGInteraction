# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SVGInteraction' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod 'Macaw', '0.9.5'

  # Pods for SVGInteraction

  target 'SVGInteractionTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SVGInteractionUITests' do
    # Pods for testing
  end

end
